<!DOCTYPE html>
<html lang="en" class="bg-zinc-900 text-white">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Parkour Athlete Entry</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    input:focus {
      outline: none;
      box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.5);
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center px-4 py-8">
  <div class="w-full max-w-md bg-zinc-800 rounded-2xl p-8 shadow-2xl">
    <h1 class="text-5xl font-bold text-center mb-3 text-blue-400">🏃‍♂️</h1>
    <h2 class="text-2xl font-bold text-center mb-6 text-blue-400">Parkour Athlete Entry Form</h2>
    <form action="/create" method="POST" class="space-y-4">
      <div>
        <label for="name" class="block text-sm mb-1">Name</label>
        <input type="text" name="name" id="name" required placeholder="Your full name"
          class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500 transition duration-200">
      </div>

      <div>
        <label for="age" class="block text-sm mb-1">Age</label>
        <input type="number" name="age" id="age" required min="10" max="100"
          class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500 transition duration-200">
      </div>

      <div>
        <label for="email" class="block text-sm mb-1">Email</label>
        <input type="email" name="email" id="email" required placeholder="you@example.com"
          class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500 transition duration-200">
      </div>

      <button type="submit"
        class="w-full py-2 mt-4 bg-blue-600 hover:bg-blue-500 text-white font-semibold rounded-xl shadow-md transition transform hover:-translate-y-0.5 hover:shadow-lg">
        Register as Athlete
      </button>
    </form>
    <a href="/read"><button
        class="w-full py-2 mt-4 bg-emerald-600 hover:bg-emerald-500 text-white font-semibold rounded-xl shadow-md transition transform hover:-translate-y-0.5 hover:shadow-lg">
        Show List
      </button></a>
  </div>
</body>

</html>