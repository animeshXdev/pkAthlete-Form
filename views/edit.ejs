<!DOCTYPE html>
<html lang="en" class="bg-zinc-900 text-white">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Athlete</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen px-4">
  <div class="bg-zinc-800 rounded-2xl p-8 w-full max-w-md shadow-xl">
    <h2 class="text-xl font-bold text-center text-yellow-400 mb-6">✏️ Update Athlete Entry</h2>
    <form action="/update/<%= athlete._id %>" method="POST" class="space-y-4">
      <div>
        <label for="name" class="block mb-1 text-sm">Name</label>
        <input type="text" name="name" id="name" required value="<%= athlete.name %>"
               class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-yellow-400 transition">
      </div>

      <div>
        <label for="age" class="block mb-1 text-sm">Age</label>
        <input type="number" name="age" id="age" required value="<%= athlete.age %>"
               class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-yellow-400 transition">
      </div>

      <div>
        <label for="email" class="block mb-1 text-sm">Email</label>
        <input type="email" name="email" id="email" required value="<%= athlete.email %>"
               class="w-full px-4 py-2 rounded-xl bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-yellow-400 transition">
      </div>

      <div class="flex justify-between">
        <a href="/read" class="text-sm text-zinc-300 hover:underline">⬅ Back to List</a>
        <button type="submit"
                class="bg-yellow-500 hover:bg-yellow-400 px-4 py-2 rounded-xl font-semibold text-zinc-900 transition">
          Update
        </button>
      </div>
    </form>
  </div>
</body>
</html>
